{
  "name": "sesame",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "api": "pnpm --filter @sesame/api",
    "web": "pnpm --filter @sesame/web",
    "schema": "pnpm --filter @sesame/schema",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "@sesame/eslint-config": "workspace:*",
    "@sesame/typescript-config": "workspace:*",
    "prettier": "^3.1.1",
    "turbo": "latest"
  },
  "packageManager": "pnpm@8.9.0",
  "engines": {
    "node": ">=18"
  }
}
